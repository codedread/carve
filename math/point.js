/**
 * Basic class for a 2D point.
 */
export class Point {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
    toString() {
        return '{x: ' + this.x + ', y: ' + this.y + '}';
    }
}
//# sourceMappingURL=point.js.map